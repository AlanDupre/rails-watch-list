<h1>Ok</h1>
<div class="class container">
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie %>
    <%= f.submit "Submit new bookmark", class: "btn btn-info" %>
  <% end %>
</div>
<%= link_to "Back to all lists", lists_path  %>